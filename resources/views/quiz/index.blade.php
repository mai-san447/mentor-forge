<x-layouts::app :title="__('クイズ')"><div class="mx-auto max-w-3xl space-y-6"><div><h1 class="text-3xl font-bold">メンタリング基礎クイズ</h1><p class="mt-2 text-zinc-600">選択肢を選び、知識を確認しましょう。</p></div><form method="POST" action="{{ route('quiz.submit') }}" class="space-y-5">@csrf @forelse($questions as $index=>$question)<fieldset class="rounded-2xl border border-zinc-200 bg-white p-6"><legend class="font-bold">{{ $index + 1 }}. {{ $question->question }}</legend><div class="mt-4 space-y-2">@foreach($question->choices as $choiceIndex=>$choice)<label class="flex cursor-pointer gap-3 rounded-lg border border-zinc-200 p-3 hover:bg-amber-50"><input type="radio" name="answers[{{ $question->id }}]" value="{{ $choiceIndex }}" required><span>{{ $choice }}</span></label>@endforeach</div></fieldset>@empty<div class="rounded-xl bg-amber-50 p-5">クイズは準備中です。</div>@endforelse @if($questions->count())<button class="rounded-lg bg-amber-500 px-5 py-2 font-semibold text-white">採点する</button>@endif</form></div></x-layouts::app>
