<x-layouts::app :title="__('トリオ練習')"><div class="mx-auto max-w-3xl space-y-6"><div><h1 class="text-3xl font-bold">トリオ練習</h1><p class="mt-2 text-zinc-600">メンター・相談者・オブザーバーの3人で練習します。</p></div><form method="POST" action="{{ route('trio.store') }}" class="space-y-4 rounded-2xl border border-zinc-200 bg-white p-6">@csrf<select name="scenario_id" required class="w-full rounded-lg border-zinc-300"><option value="">シナリオを選択</option>@foreach($scenarios as $scenario)<option value="{{ $scenario->id }}">{{ $scenario->title }}（{{ $scenario->persona->name }}）</option>@endforeach</select><div class="grid gap-3 md:grid-cols-3">@foreach(['mentor_name'=>'メンター役','mentee_name'=>'相談者役','observer_name'=>'オブザーバー役'] as $name=>$label)<label class="text-sm font-semibold">{{ $label }}<input name="{{ $name }}" required class="mt-1 w-full rounded-lg border-zinc-300"></label>@endforeach</div><button class="rounded-lg bg-indigo-700 px-5 py-2 font-semibold text-white">ルームを作成する</button></form><form method="POST" action="{{ route('trio.join') }}" class="flex gap-2 rounded-2xl bg-indigo-50 p-6">@csrf<input name="room_code" required maxlength="6" class="flex-1 rounded-lg border-indigo-200" placeholder="参加コード（6文字）"><button class="rounded-lg bg-indigo-700 px-5 py-2 font-semibold text-white">参加する</button></form></div></x-layouts::app>
