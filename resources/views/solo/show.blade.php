<x-layouts::app :title="__('ソロ練習')"><div class="mx-auto max-w-3xl space-y-5"><div><p class="text-sm font-semibold text-teal-600">SOLO ROLEPLAY</p><h1 class="mt-1 text-2xl font-bold">{{ $session->scenario->title }}</h1><p class="mt-2 text-sm text-zinc-600">{{ $session->persona->name }}：{{ $session->persona->background }}</p></div><div class="space-y-3 rounded-2xl border border-zinc-200 bg-white p-5">@foreach($session->messages as $message)<div class="flex {{ $message->speaker === 'mentor' ? 'justify-end' : 'justify-start' }}"><div class="max-w-[80%] rounded-2xl px-4 py-3 {{ $message->speaker === 'mentor' ? 'bg-teal-700 text-white' : 'bg-zinc-100 text-zinc-800' }}"><p class="mb-1 text-xs opacity-70">{{ $message->speaker === 'mentor' ? 'あなた' : $session->persona->name }}</p>{{ $message->content }}</div></div>@endforeach</div><form method="POST" action="{{ route('solo.message', $session) }}" class="flex gap-2">@csrf<input name="message" required maxlength="1000" class="flex-1 rounded-lg border-zinc-300" placeholder="相手の話を受け止めて質問しましょう"><button class="rounded-lg bg-teal-700 px-5 py-2 font-semibold text-white">送信</button></form><form method="POST" action="{{ route('solo.complete', $session) }}" class="rounded-2xl border border-amber-200 bg-amber-50 p-5">@csrf<label class="block text-sm font-semibold">振り返り（任意）</label><textarea name="reflection" class="mt-2 w-full rounded-lg border-amber-200" rows="2"></textarea><button class="mt-3 rounded-lg bg-amber-500 px-4 py-2 text-sm font-semibold text-white">練習を終了する</button></form></div></x-layouts::app>
