# アプリのルート直下。ディレクトリ一覧と機密ファイルの露出を止める
Options -Indexes

<FilesMatch "^(\.|composer\.(json|lock)$|artisan$)">
    <IfModule mod_authz_core.c>
        Require all denied
    </IfModule>
    <IfModule !mod_authz_core.c>
        Order deny,allow
        Deny from all
    </IfModule>
</FilesMatch>
